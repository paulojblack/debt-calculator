module.exports = require('./LoanPaymentLogic');
module.exports = require('./LoanDatasets');

/**
 * @typedef {Object} currentLoanDict
 *
 * @enum {Array} loans      Object containing loan properties each with array of principal/interest combos
 *
 */
